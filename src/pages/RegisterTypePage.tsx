import RegisterTypeForm from '../components/RegisterTypeForm';

const RegisterTypePage = () => {
  return (
    <div style={{ maxWidth: 500, margin: '0 auto', padding: 24 }}>
      <h2>게임 아이템 타입 등록</h2>
      <RegisterTypeForm />
    </div>
  );
};

export default RegisterTypePage;
